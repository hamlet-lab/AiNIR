{
  "name": "ainir-typescript-compile-fixture",
  "version": "0.0.0-private",
  "private": true,
  "description": "Private TypeScript compile fixture; this metadata does not define the AiNIR Python release.",
  "devDependencies": {
    "typescript": "5.8.3"
  },
  "scripts": {
    "tsc-version": "tsc --version"
  }
}
